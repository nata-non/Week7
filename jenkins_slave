pipeline {
    agent any
    
    stages {
        stage('Example') {
            steps {
                sh ("python myscript.py --age ${params.NAME ${params.Ages}}")
            }
        }
    }
}
