pipeline {
    agent any
    
    stages {
        stage('Example') {
            steps {
                sh ("python myscript.py ${params.NAME} --age ${params.Ages}")
                sh ("readFile(file: 'week7.txt')")
            }
        }
    }
}
