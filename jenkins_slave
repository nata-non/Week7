pipeline {
    agent any
    
    stages {
        stage('Example') {
            steps {
                sh ("python myscript.py ${params.NAME} --age ${params.Ages}")
                sh 'echo "some data for the file" > ./week7.txt'
            }
        }
    }
}
